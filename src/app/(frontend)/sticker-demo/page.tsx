import { InteractiveStickerCard } from "@/components/catalog/InteractiveStickerCard";

// Temporary preview route for InteractiveStickerCard. Safe to delete.
export default function StickerDemoPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-ivory">
      <InteractiveStickerCard />
    </main>
  );
}
